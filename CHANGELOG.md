# Changelog

## 0.2.0 - Unreleased

- Mark `drocsid-server::ServerError` as non-exhaustive for forward-compatible
  error handling.
- Add explicit `OutboundWriterGone` and `UnknownClient` server errors.
