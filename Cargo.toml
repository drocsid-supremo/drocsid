[package]
name = "discord"
version = "0.1.0"
edition = "2024"

[dev-dependencies.cargo-husky]
version = "1"
default-features = false
features = [
    "precommit-hook",
    "run-cargo-fmt",
    "run-cargo-clippy",
    "run-cargo-test",
]

[dependencies]
thiserror = "2"
