[package]
name = "drocsid-cli"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Command parsing and validation for drocsid."

[dependencies]
clap = { version = "4", features = ["derive"] }
drocsid-protocol = { version = "0.1.3", path = "../protocol" }
